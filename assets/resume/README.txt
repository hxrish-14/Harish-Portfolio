Optional local resume copy. The site's primary resume source is
CONFIG.RESUME_URL in config/config.js (GitHub raw file) — this folder is
just an optional fallback location if you want a local copy too.
